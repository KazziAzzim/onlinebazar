@model HomeViewModel
@{
    ViewData["Title"] = "Home";
}

<section id="fashionCarousel" class="carousel slide mb-4 shadow-sm rounded overflow-hidden" data-bs-ride="carousel">
    <div class="carousel-indicators">
        <button type="button" data-bs-target="#fashionCarousel" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
        <button type="button" data-bs-target="#fashionCarousel" data-bs-slide-to="1" aria-label="Slide 2"></button>
        <button type="button" data-bs-target="#fashionCarousel" data-bs-slide-to="2" aria-label="Slide 3"></button>
    </div>
    <div class="carousel-inner">
        <div class="carousel-item active">
            <img src="~/images/carousel/fashion-1.svg" class="d-block w-100" alt="Spring fashion collection" />
        </div>
        <div class="carousel-item">
            <img src="~/images/carousel/fashion-2.svg" class="d-block w-100" alt="Urban streetwear clothing" />
        </div>
        <div class="carousel-item">
            <img src="~/images/carousel/fashion-3.svg" class="d-block w-100" alt="Classic fashion essentials" />
        </div>
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#fashionCarousel" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#fashionCarousel" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
    </button>
</section>

@* <section class="hero mb-4">
    <h1 class="display-5 fw-bold">Shop smarter with a new OnlineBazar experience</h1>
    <p class="lead">Modern UI, curated products, and secure checkout.</p>
    <a class="btn btn-light btn-lg" asp-controller="Products" asp-action="Index">Browse Catalog</a>
</section> *@

<h2 class="h4 mb-3">Featured Products</h2>
<div class="row g-3">
@foreach (var item in Model.FeaturedProducts)
{
    <div class="col-md-4">
        <div class="card product-card h-100">
            <img class="card-img-top" src="@(item.ImageUrl ?? "https://placehold.co/600x350")" alt="@item.Name" />
            <div class="card-body">
                <h3 class="h5">@item.Name</h3>
                <p class="text-muted">@item.Description</p>
                <strong>$@item.Price</strong>
            </div>
        </div>
    </div>
}
</div>
